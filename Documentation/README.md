# Documentation

Tracked source of truth for Prism.

| Path              | What                                                  |
| ----------------- | ----------------------------------------------------- |
| `architecture.md` | Product boundaries and trust model                    |
| `specs/`          | Agreed designs                                        |
| `adr/`            | Decision records. Start at `0001-project-licence.md`. |

Sequencing and task state live in `specs/2026-08-27-implementation-plan.md`. Checkbox protocol: `[ ]` not started, `[~]` in progress, `[x]` merged.

Third-party source trees live in `References/` (not here). Restore them from the repo root lockfile.
