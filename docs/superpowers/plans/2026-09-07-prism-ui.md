# Prism UI design implementation plan

**Spec:** `docs/superpowers/specs/2026-09-07-prism-ui-design.md`  
**Date:** 2026-09-07

## Entry criteria

- [x] 2026-09-07 Design spec approved.
- [x] Existing web and extension test suites green on `master`.

## Tasks

### 1. Design docs

- [ ] Create `docs/superpowers/specs/2026-09-07-prism-ui-design.md`.
- [ ] Create `docs/superpowers/plans/2026-09-07-prism-ui.md`.

### 2. Catalogue preview fields

- [ ] Add `previewSrc` (`/previews/{id}.webp`) and `previewAlt` to `CatalogueListing` in `catalogue.ts`.
- [ ] Keep `screenshotHue` / `screenshotScene` until task 4.
- [ ] Extend `catalogue.test.ts` to assert production stats are zero and preview paths exist.

### 3. Preview assets

- [ ] Add `apps/web/public/previews/*.webp` for `kitten-ad-replace`, `youtube-home-videos`, `youtube-reddit-comments`.
- [ ] Add `apps/web/public/previews/README.md` documenting placeholder vs final captures.
- [ ] If live captures are unavailable, use minimal valid WebP placeholders labelled temporary in README.

### 4. ModShot to real images

- [ ] Replace `mod-shot.tsx` CSS scenes with `<img>` pointing at `previewSrc`.
- [ ] Remove `screenshotHue` / `screenshotScene` / `screenshotLabel` from catalogue.
- [ ] Simplify shot CSS in `globals.css`; remove scene-specific rules.
- [ ] Add tests asserting `img[src^="/previews/"]`.

### 5. Server-driven Explore

- [ ] Refactor `explore-browser.tsx`: remove `useState` filtering.
- [ ] GET form `method="get"` `action="/explore"` with `q`, `sort`, `site`.
- [ ] Pass `searchParams` from `explore/page.tsx`.
- [ ] Stable popular-sort tie-break when all installs are 0.
- [ ] Update `guest-site.test.tsx` for form presence and SSR listing.

### 6. Home steps layout and buttons

- [ ] `home.module.css`: position `.steps` top-right on desktop (`right: clamp(...)`; `left: auto`).
- [ ] `home-steps.tsx`: button styles (primary Install, secondary link-buttons); focus rings.
- [ ] Mobile: stack under blurb.

### 7. Home below-fold narrative

- [ ] `page.tsx`: three SSR sections (secure mods, default-deny, install/enable/reshape).
- [ ] Preview strip with links to `/mods/[id]`.
- [ ] Demote colour slabs to thin spectrum strip.
- [ ] Soften prism-scene hotspot for CTA contrast (`prism-scene.tsx` / `home.module.css`).

### 8. Extension popup polish

- [ ] Quiet `popup.css`: hairlines, spectrum focus accent, clearer buttons, no glow.

### 9. Verification and PR

- [ ] `npm test -w apps/web && npm test -w apps/extension`.
- [ ] Open PR with checklist: JS-off filters, honest zeros, preview placeholders noted.

## PR checklist

- [ ] Home CTAs top-right as buttons on desktop.
- [ ] Below-fold SSR sections render without JS.
- [ ] Explore filters work via query string with JS off.
- [ ] Cards and detail use `/previews/*.webp`.
- [ ] Production stats remain `EMPTY_LISTING_STATS`.
- [ ] Extension popup quieter (no glow cheese).
- [ ] Coordinator notified if preview WebPs are placeholders.
