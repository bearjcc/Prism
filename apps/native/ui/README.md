# UI

Unprivileged. Marketplace, sideload, settings, diagnostics.

Talks to the host via `packages/ipc`. Run with `--tray` at login. Right-click the tray icon opens the main window. Closing windows does not stop the host.
