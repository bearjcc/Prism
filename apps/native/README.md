# Native Prism

**Deferred (ADR 0002).** Scaffold only. Do not implement host, UI, platforms, or installer until an ADR reopens this tree.

Intended later shape: one installer, `host/` engine, `ui/` tray, `platforms/` OS adapters.
