/** Home directed scene overlays copy only when five slabs fit in a row. */
export const HOME_OVERLAY = "(min-width: 64rem)";

/** Full header links; below this the menu control is shown. */
export const SITE_NAV_WIDE = "(min-width: 60rem)";
